# Access Token и Refresh Token: Зачем нужны оба?

## 🎯 Краткий ответ

**Access Token** - для доступа к API (короткоживущий)  
**Refresh Token** - для обновления Access Token (долгоживущий, хранится в Redis)

---

## 📖 Подробное объяснение

### Access Token (Токен доступа)

**Что это:**
- Короткоживущий JWT токен (15 минут)
- Используется для доступа к защищённым ресурсам API
- Передаётся в каждом запросе в заголовке `Authorization: Bearer <token>`

**Характеристики:**
- ✅ Статeless (не хранится в Redis/БД)
- ✅ Проверяется только по подписи
- ✅ Быстрая валидация (без обращения к БД)
- ⚠️ Короткий срок жизни (15 минут)
- ⚠️ Если украден, опасен до истечения срока

**Использование:**
```typescript
// В каждом защищённом запросе
headers: {
  'Authorization': 'Bearer <accessToken>'
}
```

### Refresh Token (Токен обновления)

**Что это:**
- Долгоживущий JWT токен (7 дней)
- Используется ТОЛЬКО для получения нового Access Token
- Хранится в Redis для возможности отзыва

**Характеристики:**
- ✅ Долгий срок жизни (7 дней)
- ✅ Хранится в Redis (можно отозвать)
- ✅ Используется редко (только при обновлении токенов)
- ✅ Если украден, менее опасен (не даёт прямой доступ к API)

**Использование:**
```graphql
mutation RefreshToken($refreshToken: String!) {
  refreshToken(refreshToken: $refreshToken) {
    accessToken  # Новый access token
    refreshToken # Новый refresh token
  }
}
```

---

## 🔄 Типичный поток работы

```
1. Логин
   ├─> Получаем: accessToken (15 мин) + refreshToken (7 дней)
   └─> refreshToken сохраняется в Redis

2. Запрос к API
   ├─> Используем: accessToken в заголовке
   ├─> Сервер проверяет подпись токена
   └─> Если валиден → доступ разрешён

3. Access Token истёк (через 15 минут)
   ├─> API возвращает ошибку 401 Unauthorized
   ├─> Клиент вызывает refreshToken мутацию
   ├─> Передаём refreshToken
   ├─> Сервер проверяет refreshToken в Redis
   └─> Получаем новую пару токенов

4. Logout
   ├─> Вызываем logout мутацию
   ├─> refreshToken удаляется из Redis
   └─> Клиент удаляет оба токена локально
```

---

## 🛡️ Зачем нужны оба токена?

### Безопасность

**Проблема с одним токеном:**
- Если токен украден, злоумышленник имеет доступ до истечения срока (7 дней)
- Невозможно отозвать токен (JWT статeless)

**Решение с двумя токенами:**
- Access Token короткоживущий → минимальный ущерб при утечке
- Refresh Token можно отозвать через Redis → можно заблокировать доступ сразу
- Access Token проверяется без обращения к БД → быстрая работа

### Производительность

- Access Token валидируется без запросов к Redis/БД (только проверка подписи)
- Миллионы запросов с Access Token не нагружают Redis
- Refresh Token используется редко (раз в 15 минут), нагрузка минимальна

### UX (Пользовательский опыт)

- Пользователь не замечает обновление токенов (происходит автоматически)
- Не нужно логиниться каждые 15 минут
- При logout можно сразу отозвать все сессии через Redis

---

## 💡 Пример использования в коде

### Frontend (JavaScript/TypeScript)

```typescript
class AuthService {
  private accessToken: string | null = null;
  private refreshToken: string | null = null;

  async login(email: string, password: string) {
    const response = await fetch('/graphql', {
      method: 'POST',
      body: JSON.stringify({
        query: `
          mutation Login($data: LoginInput!) {
            login(data: $data) {
              accessToken
              refreshToken
              user { id email }
            }
          }
        `,
        variables: { data: { email, password } }
      })
    });
    
    const { login } = await response.json();
    this.accessToken = login.accessToken;
    this.refreshToken = login.refreshToken;
    localStorage.setItem('refreshToken', this.refreshToken);
  }

  async makeAuthenticatedRequest(query: string) {
    let response = await fetch('/graphql', {
      headers: {
        'Authorization': `Bearer ${this.accessToken}`
      },
      body: JSON.stringify({ query })
    });

    // Если токен истёк, обновляем
    if (response.status === 401) {
      await this.refreshTokens();
      // Повторяем запрос с новым токеном
      response = await fetch('/graphql', {
        headers: {
          'Authorization': `Bearer ${this.accessToken}`
        },
        body: JSON.stringify({ query })
      });
    }

    return response.json();
  }

  async refreshTokens() {
    const refreshToken = localStorage.getItem('refreshToken');
    const response = await fetch('/graphql', {
      method: 'POST',
      body: JSON.stringify({
        query: `
          mutation RefreshToken($refreshToken: String!) {
            refreshToken(refreshToken: $refreshToken) {
              accessToken
              refreshToken
            }
          }
        `,
        variables: { refreshToken }
      })
    });

    const { refreshToken: result } = await response.json();
    this.accessToken = result.accessToken;
    this.refreshToken = result.refreshToken;
    localStorage.setItem('refreshToken', this.refreshToken);
  }
}
```

### Backend (NestJS/GraphQL)

```typescript
// Защищённый запрос
@Query(() => UserModel)
@UseGuards(JwtAuthGuard)  // Проверяет Access Token
async getCurrentUser(@CurrentUser() user: UserModel) {
  return user;
}

// Guard автоматически:
// 1. Извлекает токен из заголовка Authorization
// 2. Проверяет подпись
// 3. Валидирует срок действия
// 4. Загружает пользователя из БД
// 5. Добавляет user в req (доступен через @CurrentUser())
```

---

## 🔐 Безопасность в текущей реализации

✅ **Что уже реализовано:**
- Refresh Token хранится в Redis (можно отозвать)
- Access Token короткоживущий (15 минут)
- При logout refresh token удаляется из Redis
- Проверка refresh token при обновлении

⚠️ **Рекомендации для продакшена:**
- Используйте HTTPS для передачи токенов
- Храните refresh token в httpOnly cookie (опционально)
- Добавьте rate limiting для refresh token endpoint
- Логируйте подозрительную активность (множественные refresh запросы)
- Рассмотрите возможность хранения blacklist устаревших access токенов (для критичных операций)

---

## 📊 Сравнительная таблица

| Характеристика | Access Token | Refresh Token |
|---------------|--------------|---------------|
| **Срок жизни** | 15 минут | 7 дней |
| **Хранение** | Память/локальное хранилище клиента | Redis (сервер) |
| **Частота использования** | Каждый запрос к API | Раз в 15 минут |
| **Проверка** | По подписи (без БД) | Проверка в Redis |
| **Можно отозвать?** | Нет (статичен) | Да (удаление из Redis) |
| **Риск при утечке** | Средний (короткий срок) | Низкий (не даёт прямой доступ) |
| **Производительность** | Высокая (без БД) | Низкая (редко используется) |

---

## ❓ Частые вопросы

**Q: Почему не использовать только Access Token?**  
A: При утечке злоумышленник имеет доступ до истечения срока. Невозможно отозвать токен.

**Q: Почему не использовать только Refresh Token?**  
A: Каждый запрос потребует обращения к Redis, что сильно замедлит работу API.

**Q: Что происходит, если Access Token украден?**  
A: Злоумышленник имеет доступ максимум 15 минут (до истечения срока).

**Q: Что происходит, если Refresh Token украден?**  
A: Злоумышленник может получать новые Access Token, но можно отозвать через logout (удаление из Redis).

**Q: Нужно ли обновлять Refresh Token?**  
A: В текущей реализации refresh token обновляется при каждом вызове refreshToken мутации (rotation), что повышает безопасность.

